<%= form_for(position) do |f| %>
  <% if position.errors.any? %>
    <div class="row col-lg-10 col-lg-offset-1 alert alert-info first-item" id="error_explanation">
      <button type="button" class="close" data-dismiss="alert">&times;</button>
      <h2><%= pluralize(drill.errors.count, "error") %> prohibited this position from being saved:</h2>

      <ul>
      <% position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-row">
    <div class="form-group col-xs-12 col-md-4">
      <%= f.label :name %>
      <%= f.text_field :name, class: "form-control", autofocus: true, placeholder:"Position Name"  %>
    </div>
  </div>
  <div class="form-group">
    <%= f.submit "Save", class: "btn btn-primary" %>
  </div>
<% end %>
